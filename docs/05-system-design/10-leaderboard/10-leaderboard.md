# 排行榜系统设计（实时 + 历史）

> 考察频率：★★★★☆  优先级：P0

## TODO（待填写）

## 1. 基础方案：Redis ZSet
- [ ] `ZADD leaderboard score userId`
- [ ] `ZREVRANK` / `ZREVRANGE` 获取名次和列表
- [ ] 时间复杂度：ZADD O(logN)，ZREVRANK O(logN)
- [ ] 内存占用：每条记录约 64 字节，100 万条 ≈ 64MB
- [ ] 完整 Go 代码示例

## 2. 超大榜单：分桶 ZSet
- [ ] 问题：ZSet 超过 100 万条时，内存和操作延迟上升
- [ ] 分桶策略：按分数段拆分（0-1000 分一个桶）
- [ ] 获取全局排名：桶内排名 + 高分桶总人数
- [ ] 分桶 Go 实现

## 3. 历史排行榜（按天/周/月）
- [ ] 每个时间周期一个 ZSet key（`leaderboard:2024-01`）
- [ ] 定时归档：crontab 生成历史快照
- [ ] 过期清理：`EXPIRE` 设置 TTL
- [ ] 增量更新 vs 全量重算

## 4. 分布式多节点写入一致性
- [ ] Redis ZSet 操作本身原子，单点足够
- [ ] 高并发写：本地聚合 + 批量提交（减少 Redis 调用次数）

## 5. 近似排名（大数据量场景）
- [ ] Count-Min Sketch：近似 TopK（误差可控）
- [ ] 适合场景：亿级用户的粗粒度排行

## 6. 数据库持久化
- [ ] Redis 只做实时计算，MySQL 持久化最终排名快照
- [ ] 分离读写：排行榜读 Redis，历史查询读 MySQL

## 高频追问
- [ ] ZSet 的底层数据结构是什么（skiplist + hash）？为什么用跳表不用红黑树？
- [ ] 如何设计"好友排行榜"（只显示好友中的排名）？
- [ ] 排行榜数据需要持久化吗？Redis 宕机怎么恢复？
