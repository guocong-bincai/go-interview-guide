# Go 编译器优化：内联与逃逸深度

> 考察频率：★★★★☆  优先级：P1

## TODO（待填写）

## 1. 函数内联（Inlining）
- [ ] 内联的本质：消除函数调用开销（参数传递、栈帧分配、跳转）
- [ ] 内联阈值：AST 节点数 ≤ 80（Go 1.17+）
- [ ] 查看内联决策：`go build -gcflags="-m"` 输出解读
- [ ] 什么导致函数不能内联：闭包、递归、`recover()`、超过阈值
- [ ] 接口方法调用为什么无法内联（动态分发）
- [ ] 强制不内联：`//go:noinline` 指令

## 2. 逃逸分析深度（补充 04-escape.md）
- [ ] `-gcflags="-m=2"` 查看详细逃逸原因
- [ ] 逃逸场景梳理：interface 赋值、闭包引用、超大栈对象、channel 传递指针
- [ ] 反直觉场景：`&x` 不一定逃逸（依赖后续使用方式）
- [ ] 逃逸对 GC 的影响（堆分配 = GC 压力）

## 3. 编译器其他优化
- [ ] 边界检查消除（BCE）：循环中的数组访问如何被优化
- [ ] 死代码消除（Dead Code Elimination）
- [ ] 常量折叠（Constant Folding）
- [ ] `sync.Pool` 为什么有效（减少 GC 扫描压力 + 复用堆对象）

## 4. 实战：如何用编译器优化提升性能
- [ ] 热路径函数保持小体积（低于内联阈值）
- [ ] 避免在热路径返回 interface（产生逃逸）
- [ ] 用 `go build -gcflags="-m" 2>&1 | grep escape` 排查意外逃逸
- [ ] benchmark 验证优化效果

## 高频追问
- [ ] interface 赋值为什么导致逃逸？
- [ ] 为什么小对象不一定分配在栈上？
- [ ] `fmt.Println` 为什么会让参数逃逸到堆上？
